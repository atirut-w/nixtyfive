#ifndef TERMINAL_H_INCLUDED
#define TERMINAL_H_INCLUDED

void print(const char *str); // Print a string to the terminal
void printbyte(const char byte); // Print a byte to the terminal
void printshort(const short word); // Print a short to the terminal
void printuuid(const char *uuid); // Print a UUID to the terminal

#endif
