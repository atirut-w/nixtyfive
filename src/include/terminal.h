#ifndef TERMINAL_H_INCLUDED
#define TERMINAL_H_INCLUDED

void print(const char *str); // Print a string to the terminal
void printbyte(char byte); // Print a byte to the terminal
void printshort(short word); // Print a short to the terminal

#endif
